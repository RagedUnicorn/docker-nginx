version: '3.7'
services:
  nginx:
    build: .
    image: ragedunicorn/nginx:1.0.0-stable
    ports:
      - "80:80"
    labels:
      com.ragedunicorn.nginx.environment: "prod"
      com.ragedunicorn.nginx.description: "Nginx container"
